import { SentimentThresholdVote } from './sentiment-threshold-vote';

describe('SentimentThesholdVote', () => {
  it('should create an instance', () => {
    expect(new SentimentThresholdVote()).toBeTruthy();
  });
});
